package com.structural.design.patterns;

import java.util.HashMap;
import java.util.Map;

/**
 * This pattern is like caching where objects are cached and when client request
 * for a object Flyweight factory will check in the map for that object if object not there
 * it will create the new and return to client and store the same to cache.
 *
 * example is Java String class String Pool implementation.
 *
 */
public class FlyweightPattern {



}
